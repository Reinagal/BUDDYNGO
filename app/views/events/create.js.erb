const formchoice = '<%= j render "choices/formthemechoice" %>'

function refreshForm(innerHTML) {
  const newEventForm = document.querySelector('.modal-content');
  newEventForm.outerHTML = innerHTML;
}

refreshForm(formchoice);

console.log("dfdfdf");

function activeThemeChoice() {
  const cards = document.querySelectorAll('.theme-choice')
  console.log(cards)
  cards.forEach((card) => {
    card.addEventListener('click', event => {
      event.target.classList.toggle('active')
    })
  })
}

activeThemeChoice();
