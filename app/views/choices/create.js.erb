function addDates() {
  const insertDates = document.querySelector('.selecteddates');
  insertDates.insertAdjacentHTML('beforeend', '<%= j render "choice_dates", choice: @choice %>');
}

addDates();
window.initFlatpickr();
